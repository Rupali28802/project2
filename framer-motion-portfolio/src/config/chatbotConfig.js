import { createChatBotMessage } from "react-chatbot-kit";

export const config = {
    boatName : "Portfolio Bot",
    initialMessages:[ 
        createChatBotMessage("Hey 👋, Welcome to My Portfolio !")

    ],
};
